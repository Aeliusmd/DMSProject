/**
 * File upload middleware placeholder.
 * Configure multer or similar when document upload endpoints are implemented.
 */
function uploadMiddleware(_req, _res, next) {
  next();
}

module.exports = uploadMiddleware;
